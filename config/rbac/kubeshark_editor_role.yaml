# This rule is not used by the project kubeshark-operator-v2 itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the kubeshark.opstreelabs.in.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kubeshark-operator-v2
    app.kubernetes.io/managed-by: kustomize
  name: kubeshark-editor-role
rules:
- apiGroups:
  - kubeshark.opstreelabs.in
  resources:
  - kubesharks
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - kubeshark.opstreelabs.in
  resources:
  - kubesharks/status
  verbs:
  - get
